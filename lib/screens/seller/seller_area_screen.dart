import 'package:flutter/material.dart';

class SellerAreaScreen extends StatelessWidget {
  const SellerAreaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SellerAreaScreen Screen'),
      ),
      body: const Center(child: Text('SellerAreaScreen Screen')),
    );
  }
}
